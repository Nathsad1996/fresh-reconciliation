<template>
  <div class="row mx-auto mb-2">
    <div class="col">Instruction with data file format (in excel and csv)</div>
  </div>
  <div class="row mx-auto mb-2">
    <div class="col">process page give user choice between csv and excel data to insert</div>
  </div>
  <div class="row mx-auto">
    <div class="col">output in csv or excel depends on user needs</div>
  </div>
</template>

<script setup>

// create user input variables that contains user data for paydrc and telco (2 variables)

// check if user choose to user csv or excel (1 variable)

// process with the right method (processExcel or processCsv) both file (paydrc ans telco)

// return the processed data to  variables (2 variables)

// choose which operations is require (check paydrc to telco or telco to paydrc) and the output format (csv or excel) (2 variables)

// calculate with calculateOutput (1 variable)

// download te output file in the user browser

// finished


</script>
